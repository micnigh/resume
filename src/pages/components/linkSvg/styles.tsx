import { default as styled } from 'styled-components';
import Link from 'gatsby-link';
import { Container as ASvg } from '../aSvg/styles';

const color = `#33f`;

export const Container = styled(ASvg.withComponent(Link))`
  
`;